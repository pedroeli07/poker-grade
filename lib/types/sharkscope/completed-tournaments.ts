/** Tipos de agregação local / payload — fonte canónica em `lib/types/sharkScopeTypes.ts`. */
export type {
  NetworkAggBucket,
  TournamentRow,
  GroupSiteBreakdownPayloadV3,
  GroupSiteBreakdownPayload,
} from "../sharkScopeTypes";
